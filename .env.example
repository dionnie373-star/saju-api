# 이 파일을 .env로 복사해서 실제 값을 채운 뒤, 배포 플랫폼(Render/Railway 등)의
# 환경변수 설정에도 똑같이 등록하세요. .env 자체는 git에 커밋되지 않습니다.

# Claude API 키 (console.anthropic.com에서 발급)
ANTHROPIC_API_KEY=

# 이메일 발송용 SMTP 정보 (SendGrid/Mailgun/Amazon SES 등 어떤 서비스든
# SMTP 릴레이를 제공하면 사용 가능. Gmail도 "앱 비밀번호"로 가능하지만
# 발송량이 많아지면 전용 트랜잭션 이메일 서비스 권장)
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=
FROM_NAME=Palja

# 랜딩페이지가 이 API를 호출할 도메인 (CORS). 배포 전 테스트 단계에서는
# 비워두면 전체 허용("*")으로 동작하고, 실사용자 오픈 전에 실제 랜딩페이지
# 도메인으로 좁히는 걸 권장합니다.
ALLOWED_ORIGIN=
